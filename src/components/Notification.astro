<div id="customNotification" class="notification-toast">
  <div class="notification-content">
    <span class="notification-icon">👀</span>
    <p id="notificationText">Ничего не найдено</p>
    <button id="closeNotificationBtn" class="close-btn" aria-label="Закрыть">&times;</button>
  </div>
</div>

<style>
  .notification-toast {
    position: fixed;
    top: 2rem;
    right: 2rem;
    background-color: rgba(30, 41, 59, 0.8);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(239, 68, 68, 0.2);
    box-shadow:
      0 20px 25px -5px rgba(0, 0, 0, 0.3),
      0 0 20px rgba(239, 68, 68, 0.05);
    border-radius: 1rem;
    padding: 1rem 1.25rem;
    z-index: 1000;
    max-width: 420px;
    transform: translateX(140%);
    transition: transform 0.45s cubic-bezier(0.16, 1, 0.3, 1);
  }

  :global(.notification-toast.visible) {
    transform: translateX(0);
  }

  .notification-content {
    display: flex;
    align-items: center;
    gap: 0.85rem;
  }

  .notification-icon {
    font-size: 1.3rem;
    flex-shrink: 0;
  }

  .notification-content p {
    margin: 0;
    font-size: 0.95rem;
    color: #f8fafc;
    line-height: 1.5;
    font-weight: 500;
  }

  .close-btn {
    background: none;
    border: none;
    color: #475569;
    font-size: 1.4rem;
    cursor: pointer;
    padding: 0 0 0 0.75rem;
    line-height: 1;
    transition: color 0.2s;
    margin-left: auto;
  }

  .close-btn:hover {
    color: #f8fafc;
  }

  @media (max-width: 480px) {
    .notification-toast {
      top: auto;
      bottom: 1.5rem;
      left: 1rem;
      right: 1rem;
      max-width: none;
      transform: translateY(150%);
    }
    :global(.notification-toast.visible) {
      transform: translateY(0);
    }
  }
</style>